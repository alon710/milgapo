import { heIL } from "./he";

// Direct export of Hebrew language strings for the application
export const t = heIL;

// We can still support multiple languages in the future if needed
export const languages = {
    he: heIL
};
